<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentSearch">

    <fragment
        android:id="@+id/fragmentSearch"
        android:name="maxeem.america.gdg.ui.GdgListFragment"
        tools:layout="@layout/fragment_gdg_list"/>

    <fragment
        android:id="@+id/fragmentAbout"
        android:name="maxeem.america.gdg.ui.AboutFragment"
        tools:layout="@layout/fragment_about"/><action android:id="@+id/action_global_fragmentAbout" app:destination="@id/fragmentAbout"/>

</navigation>